version: 2

models:
  - name: customers
    columns:
      - name: CUSTOMERID
        tests:
          - not_null
          - unique

  - name: products
    columns:
      - name: category
        tests:
          - accepted_values:
              values:
                - "Furniture"
                - "Office"                
